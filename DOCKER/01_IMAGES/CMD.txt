# 이미지 생성
docker build -t 이미지명:태그명 도커파일 위치

# 이미지 확인
docker images

# 이미지 삭제
docker rmi imageid

# 도커 이미지 내 진입
docker run -i [id or 이미지명] sh

# 임시컨테이너 생성*(종료시 삭제) + 포트 연결
docker run -it --rm -p 3000:80 fn